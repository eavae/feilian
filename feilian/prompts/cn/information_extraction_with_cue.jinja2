根据问题，从给定的上下文中提取问题中所需的信息和提示目标文本即将出现的文本，使用 JSON 呈现结果。呈现结果时，提取到的文本需做到字符级别的一致，请勿进行任何修改。给定的上下文可能不完整或不包含答案，最终结论中的JSON中禁止包含未提及的字段。

当有多个相似表达时，根据以下规则进行优先选择（value, 从高到低）：
1. 目标文本所的标签在 HTML 语义中更重要。
2. 目标文本完整地处于标签中，而不是在句子或段落中。
3. 目标文本与其他待提取的字段更接近。
4. 若这些表达能够相互补充，请都提取。

提示文本（cue, 从高到低）:
1. 提示文本：HTML中，用以提示待提取文本即将出现的标志性文本，比如`Phone number`、`地址：`等。
2. 当没有提示文本时，使用空字符串。

# 问题：
{{ query }}

# 上下文：
```html
{{ context }}
```

# 回答格式（忽略`问题`中的格式要求）：
Thought: ...(你的思考，关于上下文中提及的字段及其提示)...
结论: ...(严格遵循该JSON示例格式回答：{{ json_example }})...

# 你的回答: